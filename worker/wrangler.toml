name = "stonks-worker"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
WORKER_VERSION = "1.0.0"

# Secrets (set via wrangler secret put):
# GH_TOKEN       — GitHub PAT for stonks-data repo
# AUTH_PASSWORD  — SHA-256 hex of the app password
