name: stonks
description: Portfolio tracker — getquin clone with GitHub JSON backend.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.3
  http: ^1.2.2
  encrypt: ^5.0.3
  fl_chart: ^0.70.2
  intl: ^0.19.0
  uuid: ^4.5.1
  shared_preferences: ^2.3.4
  shimmer: ^3.0.0
  crypto: ^3.0.6
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
